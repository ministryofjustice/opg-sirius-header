# opg-sirius-header

Set up:
`yarn install`
`go mod download`

Page loads at:
`http://localhost:8889/supervision`

Build with:
`yarn build`

Down:
`make down`

Internal tests:
`go test`

Cypress:
`yarn cypress`