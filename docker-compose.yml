version: "3.6"

services:
  opg-sirius-header:
    build:
      dockerfile: docker/opg-sirius-header/Dockerfile

  cypress:
    build:
      dockerfile: docker/cypress/Dockerfile
    depends_on:
      - opg-sirius-header
